/*import { createStoreon } from 'storeon'

let counter = store => {
  store.on('@init', () => ({ category: '' }))
  store.on('inc', ({ count }) => ({ count: count + 1 }))
}

export const store = createStoreon([counter])*/